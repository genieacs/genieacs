#!/usr/bin/env node
var path = require('path');
var fs = require('fs');

var dir = path.join(path.dirname(fs.realpathSync(__filename)), '../');
process.title = 'genieacs-files';
process.chdir(dir);

require(path.join(dir, './files'));

